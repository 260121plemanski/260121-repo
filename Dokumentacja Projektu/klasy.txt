Klasy:

user: \\przechowuje dane u¿ytkownika oraz metody o wyœwietlaniu.
	private:
		string Imie, Nazwisko, email, haslo, username, numerTel;
		list<kategoria> lista;
		list<string> listaPref;
	public:
		konstruktor, \\tworzy nowego usera i jego kalendarz.
		destruktor;
	metody:
		void set<wszystko> \\do zmiany wartoœci pól. (oprócz emaila)
		string get<wszystko> \\do wyœwietlania, dla admina, do powiadomienia.
		void dodajKategorie(int kolor); \\tworzy now¹ kategoriê wywo³uj¹c konstruktor klasy kategoria, ##kolor okreœla, jaki kolor bêdzie mia³a kategoria, dla czytelnoœci.##
		void wyswGUI(); \\wyœwietla panel, jak wskazuje nazwa.
		
kalendarz: \\tworzone przez konstruktor klasy user.
	konstruktor, destruktor;
	list<wydarzenie> listaWydarzen;
	metody: 
		void dodajWydarzenie(wydarzenie a); \\dodaje wydarzenie do listaWydarzen.
		void usunWydarzenie(wydarzenie a); \\usuwa wydarzenie z listy.
		void polecWydarzenie(wydarzenie a); \\wysy³a innemu u¿ytkownikowi info o wydarzeniu.
		wydarzenie wyswietlWydarzenie(int index); \\wyœwietla wydarzenie o podanym indeksie.
		void wyslijPowiadomienie(wydarzenie a); \\pobiera jeden element z listaWydarzen i tworzy obiekt powiadomienie.
		
wydarzenie: \\pobiera info o wydarzeniu, wype³nia nimi swoje pola.
	string nazwa, typ, miejsce, godzina;
	date data;
	float cena;
	konstruktor; \\tu siê tymczasowo otwiera plik ##bazy danych zawieraj¹cej informacje o wydarzeniach. to st¹d pobierane s¹ dane##.
	metody:
		<odpowiednie typy> get<wszystko>;
	
kategoria: \\grupa wydarzeñ tworzona przez u¿ytkownika, np. filmy które zobaczê z mam¹ lub koncerty o psach.
	dziedziczy z kalendarza. czyli, taki bardziej szczegó³owy kalendarz z dodatkowymi polami (2).
	int kolor; \\dla przyjemnoœci i wygody u¿ytkownika.
	int *tab; \\przechowuje indeksy wydarzeñ z listy.
	konstruktor, destruktor;
	
administrator: \\mo¿e modyfikowaæ bazê danych.
	dziedziczy z user.
	metody:
		void wyswPanelAdm();
		void dodajWydDoBazy(wydarzenie a);
		void usunWydZBazy(wydarzenie a);
		void wyslijWarning(user a);
		void usunUsera(user a);
		void modUsera(user a);
		
powiadomienie: \\zadziwiaj¹co, wysy³a powiadomienie.
	dziedziczy z wydarzenie.
	kosntruktor, destruktor; 
	metody:
		void wyslij(string a); \\argument: jesli numer, wysyla SMSa, jesli mail, wysyla maila.